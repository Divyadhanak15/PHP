/* Write a PHP program that assigns a string to the variable $text and uses the strlen() function to display its length along with the original string. */
<?php

$text = "Divya";

echo strlen($text);

?>